# CopyTrade Admin Web (React)

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

## Notes
- Set the API base URL in the UI (default is `http://localhost:8000`).
- The app covers list/create/modify/delete for all available endpoints and shows a unified copy monitor.
