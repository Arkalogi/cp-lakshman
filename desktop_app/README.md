# CopyTrade Desktop App

## Setup

```bash
python -m pip install -r desktop_app/requirements.txt
python desktop_app/main.py
```

## API

Set the API base URL in the app header before calling endpoints.
